import React from 'react';
import {SortingContainer, SortingButton} from './styled';

const Sorting = () => (
    <SortingContainer>
        <SortingButton>Самый дешевый</SortingButton>
        <SortingButton>Самый быстрый</SortingButton>
    </SortingContainer>
);

export default Sorting;
