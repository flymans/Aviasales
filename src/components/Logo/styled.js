import styled from 'styled-components';

const LogoContainer = styled.svg`
    grid-area: hd;
    width: 89px;
    height: 89px;
    justify-self: center;
    align-self: center;
    margin: 50px auto;
`;

export default LogoContainer;
